Instruction for Replit:

The contact form on this site is no longer sending the notification email after a recent update to the phone number field's auto-formatting behavior. Please debug and fix this in the following order:

Check whether form submission is being blocked entirely. Look at the form's submit handler and any client-side validation logic. If there's a regex or validation pattern checking the phone number field's format before allowing submission, confirm it still matches the new auto-formatted output, including the case where a user has added trailing extension text like " x204" after the 10-digit number. If the validation pattern is stricter than the new format allows, the form may be silently failing validation and never reaching the email-sending step at all.
Check the browser console and server logs for errors. Submit a test entry through the form and check both the browser's developer console for any JavaScript errors at the moment of submission, and the server-side logs for any errors when the submission is received. This will confirm whether the failure is happening on the frontend (form not submitting at all) or backend (form submits but the email step fails).
Confirm the email-sending service itself is still configured correctly. Check whatever service is used to actually send the email, for example Resend, SendGrid, Nodemailer, or a similar provider, and confirm the API key or credentials are still valid and haven't expired or hit a rate limit. Also confirm the "from" and "to" email addresses in that configuration are still correct.
Test the flow end-to-end with a real submission using a phone number entered in the new masked format, including the optional extension case, and confirm that both a) the form submits successfully with a visible confirmation to the user, and b) the email actually arrives at chris@albrechtworks.com.
Once fixed, do a regression test on the whole form, not just the phone field, to confirm name, business name, email, and the message field are all still being captured and included correctly in the email that gets sent, in case the phone number logic change affected the surrounding form-handling code more broadly.

Please report back what the actual root cause was once found, particularly whether it was the validation logic or the email service itself, so we know for future changes to this form.